services:
  # Biomni Gradio UI (no GPU needed)
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: minimal  # or "full" for bio tools
    ports:
      - "${BIOMNI_PORT:-7860}:7860"
    environment:
      - BIOMNI_LLM=${BIOMNI_LLM:-qwen3.5:35b-a3b-q8_0}
      - BIOMNI_SOURCE=${BIOMNI_SOURCE:-Ollama}
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://ollama:11434}
      - BIOMNI_DATA_PATH=./data
      - BIOMNI_TIMEOUT_SECONDS=${BIOMNI_TIMEOUT_SECONDS:-600}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - BIOMNI_CUSTOM_BASE_URL=${BIOMNI_CUSTOM_BASE_URL:-}
      - BIOMNI_CUSTOM_API_KEY=${BIOMNI_CUSTOM_API_KEY:-}
    volumes:
      - biomni-data:/app/Biomni/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  # Next.js frontend
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    environment:
      - BACKEND_URL=http://backend:8000
    depends_on:
      - backend
    restart: unless-stopped

  # FastAPI backend (API + sessions + streaming)
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    ports:
      - "${API_PORT:-8000}:8000"
    # GPU visibility for nvidia-smi (system-info endpoint)
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 4G
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [utility]
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-biomni}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/orenoiomni
      - BIOMNI_LLM=${BIOMNI_LLM:-qwen3.5:35b-a3b-q8_0}
      - BIOMNI_SOURCE=${BIOMNI_SOURCE:-Ollama}
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://ollama:11434}
      - BIOMNI_DATA_PATH=./data
      - BIOMNI_TIMEOUT_SECONDS=${BIOMNI_TIMEOUT_SECONDS:-600}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
      - WORKSPACE_BASE_PATH=/data/workspaces
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - BIOMNI_CUSTOM_BASE_URL=${BIOMNI_CUSTOM_BASE_URL:-}
      - BIOMNI_CUSTOM_API_KEY=${BIOMNI_CUSTOM_API_KEY:-}
      - LOG_FORMAT=${LOG_FORMAT:-text}
    volumes:
      - biomni-data:/app/Biomni/data
      - workspaces:/data/workspaces
      - logs:/var/log/orenoiomni
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  # Celery worker (code execution)
  worker:
    build:
      context: .
      dockerfile: docker/Dockerfile.worker
    environment:
      - DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER:-biomni}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/orenoiomni
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
      - WORKSPACE_BASE_PATH=/data/workspaces
      - CELERY_CONCURRENCY=${CELERY_CONCURRENCY:-2}
      - SANDBOX_ENABLED=${SANDBOX_ENABLED:-false}
      - SANDBOX_IMAGE_PYTHON=${SANDBOX_IMAGE_PYTHON:-orenoiomni-sandbox:latest}
      - SANDBOX_IMAGE_R=${SANDBOX_IMAGE_R:-orenoiomni-sandbox:latest}
      - SANDBOX_IMAGE_BASH=${SANDBOX_IMAGE_BASH:-orenoiomni-sandbox:latest}
      - SANDBOX_CPUS=${SANDBOX_CPUS:-2.0}
      - SANDBOX_MEMORY=${SANDBOX_MEMORY:-4g}
      - SANDBOX_NETWORK_DISABLED=${SANDBOX_NETWORK_DISABLED:-true}
      - SANDBOX_TMPFS_SIZE=${SANDBOX_TMPFS_SIZE:-512m}
    volumes:
      - biomni-data:/app/Biomni/data
      - workspaces:/data/workspaces
      - logs:/var/log/orenoiomni
      # Required for sandbox execution (SANDBOX_ENABLED=true).
      # WARNING: Mounting the Docker socket gives this container root-equivalent
      # access to the host. In production, consider using a socket proxy like
      # Tecnativa/docker-socket-proxy to restrict API calls.
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 8G
    restart: unless-stopped

  # Sandbox image for ephemeral code execution containers.
  # Not a running service — only used to build the image.
  # Build with: docker compose build sandbox
  sandbox:
    build:
      context: .
      dockerfile: docker/Dockerfile.sandbox
    image: orenoiomni-sandbox:latest
    profiles: ["build-only"]

  # Ollama model server (GPU required)
  # Use: docker compose --profile gpu up
  # Or run Ollama on host and set OLLAMA_BASE_URL=http://host.docker.internal:11434
  ollama:
    image: ollama/ollama:latest
    profiles: ["gpu"]
    ports:
      - "${OLLAMA_PORT:-11434}:11434"
    environment:
      - OLLAMA_KEEP_ALIVE=-1
      - OLLAMA_FLASH_ATTENTION=1
      - OLLAMA_KV_CACHE_TYPE=q8_0
    volumes:
      - ollama-models:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s
    restart: unless-stopped

  # PostgreSQL
  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=orenoiomni
      - POSTGRES_USER=${POSTGRES_USER:-biomni}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-biomni}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Redis (Celery broker + result backend)
  redis:
    image: redis:7-alpine
    command: >
      sh -c 'if [ -n "$$REDIS_PASSWORD" ]; then
        redis-server --requirepass "$$REDIS_PASSWORD" --appendonly yes;
      else
        redis-server --appendonly yes;
      fi'
    ports:
      - "${REDIS_PORT:-6379}:6379"
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ${REDIS_PASSWORD:+-a $REDIS_PASSWORD} ping | grep -q PONG"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  biomni-data:
  ollama-models:
  postgres-data:
  redis-data:
  workspaces:
  logs:
